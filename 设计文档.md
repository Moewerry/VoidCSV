## 项目：VoidCSV 大文件 CSV 浏览器

### 背景与目标
- 你当前使用 `VSCode` 打开 `2GB+` 的 CSV 会卡顿/无响应。
- 目标是做一个“网页端专门浏览大文件 CSV”的工具：尽量不崩溃、支持快速翻页/虚拟滚动、并提供混合策略以兼顾体验与性能。

### 非目标（先约束范围）
- 不追求在浏览器端完成复杂 ETL（清洗、导出超大子集等）到极致。
- 不追求对所有奇葩 CSV（极端编码/不规范转义）都完美兼容，但需要有降级策略与错误提示。

---

## 总体架构（混合版）

### 组件分层
1. 前端 Web UI（“高级黑色”主题）
   - 文件选择、模式提示、小文件解析结果展示、大文件索引进度展示
   - 表格渲染（虚拟滚动）、列信息面板、过滤/搜索入口
2. 浏览器小文件解析层（纯前端模式）
   - Web Worker + 流式解析
   - 只解析用户需要的展示范围（通过分页/行游标机制）
3. 本地引擎（大文件模式）
   - 作为“运行在你电脑上的本地服务”（建议：Node.js + 流式 CSV 解析库）
   - 支持：流式上传（或桌面包装直接读文件）、建立行索引、按行区间返回数据、支持简单搜索/统计
4. 索引与缓存
   - 前端模式：IndexedDB（可选）缓存解析进度或列推断结果
   - 本地引擎模式：磁盘索引文件 + 索引状态（可续传/可恢复）

### 混合版策略
- 自动检测文件大小（来自浏览器 `File.size`）
- 默认阈值建议：`200MB`（可在 UI 里可配置）
- 策略：
  - `<= 阈值`：纯前端模式（不启动本地引擎）
  - `> 阈值`：提示“文件较大，建议启用本地引擎以获得可用的随机访问与更低的浏览器压力”，用户确认后启用大文件模式

### 为什么需要本地引擎
- 浏览器对 2GB+ 的 CSV：随机访问、稳定的流式解析与可控内存非常困难。
- 本地引擎可以：
  - 以更合适的方式流式扫描并构建“行号 -> 文件字节偏移”的索引
  - 按区间返回数据给前端，从而实现“快速翻页/虚拟滚动”

---

## 关键数据流（用户浏览流程）

### 小文件模式（纯前端）
1. 用户选择 CSV 文件
2. 前端在 Web Worker 内流式解析
3. 解析过程中只保留：
   - 表头（如果有）
   - 展示区需要的数据行（虚拟滚动依赖“行游标”，必要时做分段解析）
4. 表格组件根据滚动位置请求“下一段数据”（由 Worker 继续向前解析或在缓存命中时直接返回）

### 大文件模式（启用本地引擎）
1. 用户选择 CSV 文件，并触发“大文件模式”
2. 前端发起到本地引擎的上传（`localhost`）：
   - 使用流式上传避免一次性把 2GB 全塞进内存
3. 本地引擎一边写入临时文件，一边流式解析并建立索引：
   - 索引粒度建议：每 `1000` 行记录一次“起始字节偏移”
   - 同时做基础元数据提取：列名、列数量、分隔符推断、编码推断（至少提示用户可能需要指定编码）
4. 前端表格虚拟滚动时，根据“当前可视行范围”请求行区间：
   - 引擎使用索引定位到附近偏移
   - 从该偏移开始流式解析到目标行区间
   - 以 JSON 返回给前端（只返回可视范围）
5. 用户可以继续滚动、搜索、或跳转到行号

---

## UI/UX 设计（“高级黑色”主题）

### 主题风格要求
- 深色背景（接近黑曜石）、低对比文字、强调色用于交互（例如青绿/蓝紫渐变均可）
- 表头固定、行 hover 高亮、选中行细边框
- 操作区域（工具栏/提示条）使用更高对比的底色与圆角面板

### 页面布局（推荐）
1. 顶部：文件选择按钮 + 模式提示条 + 状态（解析中/索引中/就绪）
2. 左侧（可折叠）：列信息（列名、类型推断概览、缺失值概览）
3. 中间：大表格（虚拟滚动）
4. 右侧（可选）：搜索/过滤条件面板（搜索建议仅对“当前列”或“已索引部分”）
5. 底部：分页/行号跳转（“跳到第 N 行”）

### 混合版提示文案（示例）
- 文件大小超过阈值，建议启用本地引擎以获得更流畅的浏览体验（随机翻页/虚拟滚动）。
- 启用后将进行上传与索引构建（可取消），完成后即可快速浏览。

---

## 功能方案

### 必做功能（MVP）
1. 文件选择与大小检测
2. 表格虚拟滚动（小/大模式都可用）
3. 小文件纯前端模式：流式解析 + 分段展示
4. 大文件本地引擎模式：
   - 索引构建进度条
   - 根据滚动位置请求行区间并渲染
5. 列表头展示、单元格基础渲染（超长值截断 + 悬停展开）

### 搜索/跳转（逐步增强）
1. 行号跳转：输入行号直接请求该区间（需要索引支持）
2. 简单搜索（建议先限制范围）：
   - 支持“按列搜索（包含关系）”
   - 搜索结果返回匹配行号列表或滚动到第一个匹配处
3. 排序（先做轻量版）
   - 先只支持“前端当前缓存范围排序”
   - 或提供“对整表排序”作为可选项（大文件成本较高，需要引擎侧支持）

### 元数据与列推断（轻量）
1. 分隔符推断（`,`, `;`, `\t` 等）+ 解析失败的兜底提示
2. 列数/列名识别（如果有表头）
3. 基础类型推断（字符串/数字/日期仅做展示级别推断，不强制强类型）

---

## 本地引擎设计（大文件模式）

### 部署形态选择
- 方案 A（推荐启动简单）：本地 Node 服务（监听 `http://127.0.0.1:<port>`）
  - 浏览器将文件流式上传到本地引擎
  - 引擎落盘并建立索引
- 方案 B（更高级体验）：Electron/桌面壳
  - 浏览器前端直接读取本地文件路径并交给后端进程
  - 可避免浏览器上传 2GB，但实现成本更高

本设计文档默认先按“方案 A”写接口与流程，后续可再升级。

### 核心数据结构
1. 索引表（Index）
   - 记录：`lineNumber -> byteOffset`（或者“块索引”）
   - 粒度：每 `N` 行一个锚点（建议 `N=1000`，可配置）
2. 文件状态（状态机）
   - `idle / uploading / indexing / ready / error / cancelled`
3. 列解析配置（ParseConfig）
   - 分隔符、是否有表头、引号规则、编码（如果可确定）

### 必需接口（示例）
1. `POST /api/upload`
   - 流式接收文件（body: stream）
   - 返回：`uploadId`
2. `GET /api/status?uploadId=...`
   - 返回：上传进度、索引进度、元数据（列数等）
3. `GET /api/rows?uploadId=...&start=...&count=...`
   - 返回：`rows`（数组二维/数组对象）、`columns`（如需）
4. `GET /api/stats?uploadId=...`
   - 返回：行数估计/准确行数（可做最终统计）、类型推断概览
5. `POST /api/search`
   - 入参：`columnIndex`、`query`、`limit`
   - 返回：匹配行号列表或首个匹配位置

### 解析正确性要点（非常重要）
- 不能用“按字节遇到换行就切行”的粗糙方式，因为 CSV 引号字段可能包含换行。
- 引擎必须使用“支持引号与转义、可流式解析”的成熟 CSV parser。
- 索引锚点的“起始偏移”需要与“正确的行边界”对齐（引擎侧实现时要从锚点附近重新解析对齐到目标行号）。

### 性能与资源策略
1. 索引建立阶段允许渐进式完成（先有可用浏览，再增强搜索能力）
2. 前端请求只返回“当前可视范围”的行，避免大 JSON 一次性返回
3. 解析与索引任务应在引擎的 worker/线程中执行，避免阻塞 API

---

## 编码与兼容性建议

### CSV 编码
- 浏览器端对编码推断能力有限，建议在 UI 提供编码下拉（例如 `utf-8 / gb18030 / gbk`）作为“失败后兜底”

### 换行与分隔符
- 兼容 `\n` / `\r\n`
- 分隔符推断失败时提供手动选择：`,` / `;` / `\t` / `|` 等

---

## 验收标准（你最终应该能得到什么）
1. 能打开并浏览 `2GB+` CSV：滚动不会明显卡死
2. 表格渲染是“虚拟滚动”，只渲染可视行
3. 大文件模式下能看到“上传/索引进度”，完成后能快速翻页/跳转行号
4. 小文件模式无需本地引擎也能用

---

## 里程碑（建议开发顺序）
1. Web UI：高级黑色主题 + 文件选择 + 虚拟滚动表格骨架
2. 小文件纯前端：Web Worker + 流式解析 + 基础行展示
3. 本地引擎 v1：上传、状态、索引构建、`/rows` 区间返回
4. 大文件模式接入：前端根据滚动请求区间数据
5. 基础搜索与行号跳转（先可用，再优化体验）

---

## 风险与对策
1. CSV 解析正确性风险（引号内换行、转义引号）
   - 对策：使用成熟解析库；解析失败给出可操作的提示（分隔符/编码/表头）
2. 索引构建耗时
   - 对策：允许“先可浏览后增强功能”（例如先有行区间读取，再做更复杂搜索索引）
3. 浏览器渲染压力
   - 对策：虚拟滚动 + 单元格截断 + 限制同时渲染列数（必要时提供列筛选）

---

## 需要你确认的 4 个选择（用于落地实现）
1. 本地引擎你更倾向方案 A（浏览器上传到 `localhost`）还是方案 B（Electron 直接读文件）？
2. CSV 的典型格式：大多数是否“逗号分隔、UTF-8、带表头”？
3. 你希望搜索/排序做到什么程度：只看当前列的包含搜索，还是需要全表级？
4. 阈值 `200MB` 是否接受，还是你希望更激进/更保守？

---

## 首页与进入流程（界面逻辑设计）

### 页面划分（建议）
- `Home`：首页，仅展示项目标题与一句话介绍 + “进入浏览器”按钮
- `Viewer`：真正的 CSV 浏览页面（文件选择、阈值混合提示、本地引擎状态、虚拟表格等）

### 路由设计（SPA 约定）
- 路由 `GET /`：`Home`
- 路由 `GET /viewer`：`Viewer`
- 从 `Home` 进入时，只做一次路由跳转，不在首页触发解析或上传

### 首页 `Home` 的状态机（尽量保持纯静态）
- `home.init`（页面首次加载）
  - 动作：读取用户偏好（可选）：阈值、是否默认启用本地引擎提示的“上次选择”
  - UI：渲染深色“高级黑色”主题背景、标题、介绍、CTA 按钮
- `home.wait_click`
  - UI：CTA 按钮常驻可点击
  - 不做任何文件解析、不开 Worker、不请求引擎

### 进入按钮（CTA）点击逻辑
1. 点击 `进入浏览器` 按钮
2. 执行动作：`navigate('/viewer')`
3. 写入（可选）浏览上下文到内存或 `sessionStorage`
   - `entryFrom: 'home'`
   - `defaultThreshold: <阈值>`（若用户之前改过，可从本地读取）
   - `defaultEnginePreference: 'auto' | 'forceLocal'`（若你希望在 Viewer 里继续沿用用户偏好）
4. 进入 `Viewer` 后由 `Viewer` 自己启动流程
   - 例如：等待用户选择文件后，基于 `File.size` 决定“小文件纯前端”还是“大文件提示启用本地引擎”

### Viewer 页面对进入状态的读取（对齐逻辑）
- `Viewer` 在挂载时读取：
  - `defaultThreshold`
  - `defaultEnginePreference`（若存在）
  - 若无则走默认策略：`threshold=200MB`，大于阈值先提示“建议启用本地引擎”
- 这样可以保证首页保持轻量，且用户偏好在体验上是连续的

### 错误与降级（首页层面）
- 首页不涉及文件处理，因此几乎不存在错误态
- 仅处理路由失败/运行环境异常（可选）
  - 若路由跳转失败，按钮可提示“进入失败，请刷新后重试”

### 简化的事件流（顺序）
- `Home.mount` -> 渲染标题/介绍/按钮
- `Home.clickCTA` -> `navigate('/viewer')` -> （可选）写入 sessionStorage
- `Viewer.mount` -> 读取默认阈值/偏好 -> 等待用户选择文件 -> 根据大小触发混合策略
